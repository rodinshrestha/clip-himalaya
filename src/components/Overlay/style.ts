'use client';

import styled from 'styled-components';

export const StyledDiv = styled.div`
  &.overlay-wrapper {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
    z-index: 1;
  }
`;
