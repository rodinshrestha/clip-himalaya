'use client';

import styled from 'styled-components';

export const StyledDiv = styled.div`
  margin-top: 40px;

  .section-title {
    text-align: center;
    margin-bottom: 20px;
  }
  .trek-wrapper {
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
  }
`;
