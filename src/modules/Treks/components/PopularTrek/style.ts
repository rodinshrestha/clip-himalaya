'use client';

import styled from 'styled-components';

export const StyledDiv = styled.div`
  padding: 40px 0;

  .section-title {
    text-align: center;
    margin-bottom: 20px;
  }
  .trek-wrapper {
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
    row-gap: 60px;
  }
`;
